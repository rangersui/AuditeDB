[package]
name = "elastik-bin"
version = "8.2.0"
edition = "2021"
description = "AuditeDB server — HTTP, CoAP, MQTT, SSE adapters for the Elastik L5 Engine."
license = "MIT"
repository = "https://github.com/rangersui/AuditeDB"
homepage = "https://github.com/rangersui/AuditeDB"
publish = false

[dependencies]
elastik-core = { path = "../core", default-features = false, features = ["unstable-engine"] }
axum = { version = "0.8", default-features = false, features = ["http1", "tokio"] }
base64 = "0.22"
bytes = "1"
futures-util = "0.3"
percent-encoding = "2"
tokio = { version = "1", features = ["macros", "rt", "sync", "time", "net", "signal"] }
tracing = "0.1"
rumqttd = { version = "=0.20.0", optional = true, default-features = false }

[features]
default = ["bundled-sqlite", "coap", "multi-thread", "unstable-engine"]
bundled-sqlite = ["elastik-core/bundled-sqlite"]
coap = []
multi-thread = ["tokio/rt-multi-thread"]
mqtt = ["dep:rumqttd", "tokio/io-util"]
unstable-engine = []

[dev-dependencies]
base64 = "0.22"
hex = "0.4"
rusqlite = { version = "0.40", default-features = false }
tower = { version = "0.5", features = ["util"] }

[[bin]]
name = "elastik-core"
path = "src/main.rs"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
panic = "abort"

[profile.rut241]
inherits = "release"
opt-level = "z"
lto = "fat"
codegen-units = 1
strip = "symbols"
panic = "abort"
