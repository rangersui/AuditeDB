name: ci

on:
  pull_request:
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  rust-core:
    name: rust core checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: version consistency
        run: python tools/version_consistency_check.py
      - name: audit chain verifier self-test
        run: python tools/audit_chain_verify.py --self-test
      - name: format
        working-directory: core
        run: cargo fmt --check
      - name: binary format
        working-directory: bin
        run: cargo fmt --check
      - name: core no structured data
        run: python core/no_json.py core/src
      - name: core contraband
        run: >
          python core/contraband.py
          --only no_yaml
          --only no_toml_parse
          --only no_last_modified
          --only no_hardcoded_secrets
          core/src
      - name: core production clippy doctrine lint
        working-directory: core
        run: cargo clippy --lib -- -D clippy::unwrap_used -D clippy::print_stdout
      - name: binary production clippy doctrine lint
        working-directory: bin
        run: cargo clippy --bin elastik-core -- -D clippy::unwrap_used -D clippy::print_stdout
      - name: core production clippy doctrine lint minimal
        working-directory: core
        run: cargo clippy --lib --no-default-features --features bundled-sqlite,unstable-engine -- -D clippy::unwrap_used -D clippy::print_stdout
      - name: binary production clippy doctrine lint minimal
        working-directory: bin
        run: cargo clippy --bin elastik-core --no-default-features --features bundled-sqlite,unstable-engine -- -D clippy::unwrap_used -D clippy::print_stdout
      - name: clippy
        working-directory: core
        run: cargo clippy -- -D warnings
      - name: binary clippy
        working-directory: bin
        run: cargo clippy -- -D warnings
      - name: rustdoc warnings
        working-directory: core
        run: RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --lib
      - name: unit tests
        working-directory: core
        run: cargo test
      - name: binary tests
        working-directory: bin
        run: cargo test

  sdk-blackbox:
    name: sdk blackbox (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-2025-vs2026
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: python sdk blackbox
        run: python sdk/tests/e2e_blackbox.py
      - name: trusted shell pool
        run: python sdk/tests/test_tools.py
      - name: python compileall
        run: python -m compileall -q sdk/src sdk/tests
